<%- include('.././header.ejs') %>
  <h1>Update Restaurant</h1>

  <form action="/adminux/update-restaurant/<%= restaurant._id %>" method="post" enctype="multipart/form-data">
    <div class="form-group">
      <label for="username">Username</label>
      <input type="text" class="form-control" id="username" name="username" value="<%= restaurant.username %>" required
        minlength="5" maxlength="40">
    </div>
    <div class="form-group">
      <label for="name">Name</label>
      <input type="text" class="form-control" id="name" name="name" value="<%= restaurant.name %>" required
        minlength="5" maxlength="40">
    </div>

    <div class="form-group">
      <label for="email">Email</label>
      <input type="email" class="form-control" id="email" name="email" value="<%= restaurant.email %>" required>
    </div>

    <div class="form-group">
      <label for="password">Password</label>
      <input type="password" class="form-control" id="password" name="password">
    </div>

    <div class="form-group">
      <label for="reEnterPassword">Re-enter Password</label>
      <input type="password" class="form-control" id="reEnterPassword" name="reEnterPassword">
    </div>

    <div class="form-group">
      <label for="phone">Phone</label>
      <input type="text" class="form-control" id="phone" name="phone" value="<%= restaurant.phone %>">
    </div>

    <div class="form-group">
      <label for="address">Address</label>
      <input type="text" class="form-control" id="address" name="address" value="<%= restaurant.address %>" required>
    </div>

    <!-- Add more form fields for other properties -->
      <!-- Option to delete the existing image -->
  <div class="form-group">
    <label for="deleteImage">Delete Image</label>
    <input type="checkbox" id="deleteImage" name="deleteImage" value="true">
    <label for="deleteImage">Check this box to delete the existing image</label>
  </div>

  <!-- Option to upload a new image -->
  <div class="form-group">
    <label for="image">Upload New Image</label>
    <input type="file" class="form-control" id="image" name="image">
  </div>
  <img src="/images/restaurant/banner/<%= restaurant.image %>" alt="">

    <button type="submit" class="btn btn-primary" onclick="updateRestaurant('<%= restaurant._id %>')">Update Restaurant</button>
  </form>
  <a href="/adminux" type="button" class="btn btn-warning text-decoration-none">Back</a>  

  <script src="/js/admin/update-restaurant.js"></script>            
  <%- include('.././footer.ejs') %>